package-lock.json
node_modules
start.sh
dist
release
# cache.json (removed to allow pre-generated cache)
.bridge-cache
CLAUDE.md
linkedin

# Secrets / local-only
.env
.env.*
*.pem
*.key

# Editor / OS noise
.DS_Store
.idea/
*.log
coverage/

# Local verification scaffolding: throwaway browser-driver scripts and the
# sample files they run against. Never commit these — the sample files are
# whatever the person testing happened to have, and can be large or private.
__*.mjs
.verify-tmp/
.e2e-fixture.pdf
